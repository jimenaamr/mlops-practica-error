apiVersion: v1
kind: Service
metadata:
  name: mlops-web-service-sinerror
spec:
  selector:
    app: mlops-web-sinerror
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501
  type: LoadBalancer